package ${packageName};

import ${applicationPackage}.base.BasePresenter
import ${applicationPackage}.base.OnProgress

/**
 * 创建者： 
 * 时间：  
 */
class ${presenterClass}(progress: OnProgress) : BasePresenter(progress) {

}